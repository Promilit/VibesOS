import { Icon } from '@tabler/icons-react';
export declare function NavMain({ items, }: {
    items: {
        title: string;
        url: string;
        icon?: Icon;
    }[];
}): import("react").JSX.Element;
